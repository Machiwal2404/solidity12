// SPDX-License-Identifier: MIT

pragma solidity >=0.5.0<0.9.0;

contract lottery {
    address public manager;
    address payable[] public participants ;

    constructor()
    {
        address = msg.sender;
    }
    receive() external public payable
    {
        participants.push(payable(msg.sender));
    }
}
